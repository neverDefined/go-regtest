name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    name: Tests (Go 1.24, Ubuntu)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        # The library still targets Go 1.23 (see go.mod), but the test
        # toolchain runs on 1.24 so govulncheck doesn't flag stdlib CVEs
        # that were fixed in 1.24.x. Phase 3.2 will expand to a matrix.
        go-version: '1.24'
        cache: true

    - name: Install Bitcoin Core
      run: |
        sudo snap install bitcoin-core
        sudo ln -sf /snap/bitcoin-core/current/bin/bitcoind /usr/local/bin/bitcoind
        sudo ln -sf /snap/bitcoin-core/current/bin/bitcoin-cli /usr/local/bin/bitcoin-cli

    - name: Verify Bitcoin Core installation
      run: |
        which bitcoind
        bitcoind --version

    - name: Install development tools
      run: make install-tools

    - name: Run all checks
      run: make ci

    - name: Run govulncheck
      run: make vuln

    - name: Generate coverage report
      run: |
        echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Coverage Summary" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        go tool cover -func=coverage.out >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Coverage Percentage" >> $GITHUB_STEP_SUMMARY
        COVERAGE=$(go tool cover -func=coverage.out | tail -1 | awk '{print $3}' | sed 's/%//')
        echo "**Total Coverage: ${COVERAGE}%**" >> $GITHUB_STEP_SUMMARY

    - name: Upload coverage artifacts
      uses: actions/upload-artifact@v5
      with:
        name: coverage-report
        path: |
          coverage.out
          coverage.html
        retention-days: 30